import { NumberFormatInputPipe } from './number-format-input.pipe';

describe('NumberFormatInputPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberFormatInputPipe();
    expect(pipe).toBeTruthy();
  });
});
